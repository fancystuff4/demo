"use strict";

var _require = require('./babel-config-utils'),
    getBabelConfig = _require.getBabelConfig;

module.exports = function (api) {
  api.cache.forever();
  return getBabelConfig('esm');
};