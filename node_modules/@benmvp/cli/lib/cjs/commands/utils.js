"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.spawnAsync = void 0;

var _child_process = require("child_process");

var spawnAsync = function spawnAsync(command, args) {
  return new Promise(function (resolve, reject) {
    var childProcess = (0, _child_process.spawn)(command, args, {
      stdio: 'inherit'
    });
    childProcess.on('close', function (code) {
      if (code !== 0) {
        reject(new Error("\"".concat(command, " ").concat(args.join(' '), "\" exited with code ").concat(code)));
      } else {
        resolve();
      }
    });
  });
};

exports.spawnAsync = spawnAsync;