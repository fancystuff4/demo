import { START_ARGS } from '../../cli/args';
import testCommand from '../test';
/**
 * Runs the specified modes of tests in on-going watch mode during active development
 * @param {Object} [options] The configuration options for running the library
 * @param {TestMode[]} [options.modes] List of the types or modes of tests to run
 * @param {string} [options.pattern]  Regexp pattern string that is matched against
 *  all tests paths before executing the test
 * @returns {Promise<Result>} The result of executing the start
 */

export default (function () {
  var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
      _ref$modes = _ref.modes,
      modes = _ref$modes === void 0 ? START_ARGS.modes.default : _ref$modes,
      _ref$pattern = _ref.pattern,
      pattern = _ref$pattern === void 0 ? START_ARGS.pattern.default : _ref$pattern;

  return testCommand({
    modes: modes,
    pattern: pattern,
    watch: true
  });
});